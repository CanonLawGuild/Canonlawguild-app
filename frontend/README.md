# Frontend (React + TypeScript)

This is a minimal Vite + React + TypeScript frontend scaffold that imports the shared stylesheet at `static/css/styles.css`.

Install and run:

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173` after `vite` starts.
